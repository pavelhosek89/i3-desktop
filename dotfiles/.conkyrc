conky.config = {
	background = false,
	cpu_avg_samples = 2,
	default_color = 'white',
	double_buffer = false,
	out_to_console = true,
	out_to_x = false,
	own_window = false,
	short_units = true,
	total_run_times = 0,
	update_interval = 0.5,
	use_spacer = 'left'
}

conky.text = [[
[{ "full_text" : "   ${if_mpd_playing}${mpd_artist} - ${mpd_title}${else}mpd is stopped${endif} ", "color" : "\#657b83" },
 { "full_text" : "\t"},
 { "full_text" : " ${time %a %e %b, %I:%M %P} "},
 { "full_text" : "|"},
 { "full_text" : "   ${exec amixer -c 1 -M -D pulse get Master | grep -m 1 -o -E \[\[:digit:\]\]+% | tr -d "%"}", "color" : "\#dc322f" }],
]]
